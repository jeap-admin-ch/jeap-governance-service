spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    out-of-order: true
  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
    properties:
      hibernate:
        format_sql: true
